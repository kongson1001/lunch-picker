# Chat Feature Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 방 안에서 실시간 채팅 가능 — 내 메시지 삭제, 풀 너비 배치

**Architecture:** Firebase Realtime DB의 `rooms/{roomId}/messages`에 메시지 저장. `Chat.jsx` 컴포넌트가 해당 경로를 실시간 구독하고, Room.jsx 2컬럼 아래 풀 너비로 렌더링.

**Tech Stack:** React 19, Firebase Realtime Database (onValue/push/remove), CSS (App.css)

---

### Task 1: Firebase에 messages 헬퍼 추가

**Files:**
- Modify: `src/utils/room.js`

**Step 1: sendMessage, deleteMessage 함수 추가**

```js
// src/utils/room.js 하단에 추가
export async function sendMessage(roomId, user, text) {
  const messagesRef = ref(db, `rooms/${roomId}/messages`);
  const newRef = push(messagesRef);
  await set(newRef, {
    uid: user.uid,
    nickname: user.nickname,
    profileImage: user.profileImage || null,
    text: text.trim(),
    createdAt: Date.now(),
  });
}

export async function deleteMessage(roomId, messageId) {
  await remove(ref(db, `rooms/${roomId}/messages/${messageId}`));
}
```

`push`, `set`, `remove`는 이미 import되어 있음 — 추가 import 불필요.

**Step 2: 로컬 개발 서버 기동 확인**

```bash
npm run dev
```

콘솔 에러 없으면 OK.

**Step 3: Commit**

```bash
git add src/utils/room.js
git commit -m "feat: add sendMessage and deleteMessage helpers"
```

---

### Task 2: Chat.jsx 컴포넌트 생성

**Files:**
- Create: `src/components/Chat.jsx`

**Step 1: 컴포넌트 작성**

```jsx
import { useState, useEffect, useRef } from 'react';
import { db, ref, onValue } from '../firebase';
import { sendMessage, deleteMessage } from '../utils/room';

export default function Chat({ roomId, user }) {
  const [messages, setMessages] = useState([]);
  const [text, setText] = useState('');
  const [sending, setSending] = useState(false);
  const bottomRef = useRef(null);

  useEffect(() => {
    const messagesRef = ref(db, `rooms/${roomId}/messages`);
    const unsubscribe = onValue(messagesRef, (snapshot) => {
      const data = snapshot.val();
      if (!data) {
        setMessages([]);
        return;
      }
      const list = Object.entries(data)
        .map(([id, msg]) => ({ id, ...msg }))
        .sort((a, b) => a.createdAt - b.createdAt);
      setMessages(list);
    });
    return () => unsubscribe();
  }, [roomId]);

  // 새 메시지 도착 시 자동 스크롤
  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  const handleSend = async () => {
    const trimmed = text.trim();
    if (!trimmed || sending) return;
    setSending(true);
    try {
      await sendMessage(roomId, user, trimmed);
      setText('');
    } finally {
      setSending(false);
    }
  };

  const handleKeyDown = (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  const handleDelete = async (messageId) => {
    await deleteMessage(roomId, messageId);
  };

  return (
    <section className="chat-section">
      <h2 className="section-title">채팅</h2>
      <div className="chat-messages">
        {messages.length === 0 && (
          <p className="chat-empty">첫 메시지를 보내보세요!</p>
        )}
        {messages.map((msg) => {
          const isMine = msg.uid === user?.uid;
          return (
            <div key={msg.id} className={`chat-message ${isMine ? 'mine' : 'others'}`}>
              {!isMine && (
                <div className="chat-avatar">
                  {msg.profileImage
                    ? <img src={msg.profileImage} alt="" />
                    : <div className="chat-avatar-placeholder">{msg.nickname[0]}</div>
                  }
                </div>
              )}
              <div className="chat-bubble-wrap">
                {!isMine && <span className="chat-nickname">{msg.nickname}</span>}
                <div className="chat-bubble-row">
                  {isMine && (
                    <button className="chat-delete-btn" onClick={() => handleDelete(msg.id)}>
                      삭제
                    </button>
                  )}
                  <div className="chat-bubble">{msg.text}</div>
                </div>
              </div>
            </div>
          );
        })}
        <div ref={bottomRef} />
      </div>
      <div className="chat-input-row">
        <input
          type="text"
          className="chat-input"
          value={text}
          onChange={(e) => setText(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="메시지를 입력하세요"
          disabled={sending}
          maxLength={300}
        />
        <button
          className="chat-send-btn"
          onClick={handleSend}
          disabled={sending || !text.trim()}
        >
          전송
        </button>
      </div>
    </section>
  );
}
```

**Step 2: Commit**

```bash
git add src/components/Chat.jsx
git commit -m "feat: add Chat component with realtime messages and delete"
```

---

### Task 3: Room.jsx에 Chat 연결

**Files:**
- Modify: `src/pages/Room.jsx`

**Step 1: import 추가 (기존 import 블록 하단)**

```jsx
import Chat from '../components/Chat';
```

**Step 2: JSX에 Chat 추가 — `</div>` (room-body 닫기) 바로 앞에 삽입**

`room-body` div 닫는 태그 바로 위, 즉 두 번째 `</section>` 아래에 추가:

```jsx
      </div>  {/* .room-body 닫기 */}

      <Chat roomId={roomId} user={user} />
```

현재 Room.jsx의 JSX 구조:
```jsx
<div className="room-container">
  <header>...</header>
  <div className="room-body">        ← 2컬럼
    <section>투표</section>
    <section>검색</section>
  </div>                             ← 여기 아래에 Chat 추가
  <Chat roomId={roomId} user={user} />
</div>
```

**Step 3: 브라우저에서 확인**

- `/room/:id` 진입 시 채팅 섹션이 하단에 표시되는지 확인
- 메시지 전송 → Firebase에 저장 → 실시간으로 목록에 표시 확인
- 내 메시지에만 삭제 버튼 표시 확인

**Step 4: Commit**

```bash
git add src/pages/Room.jsx
git commit -m "feat: integrate Chat into Room page"
```

---

### Task 4: 채팅 CSS 스타일 추가

**Files:**
- Modify: `src/App.css`

**Step 1: App.css 하단에 추가**

```css
/* ===== Chat ===== */
.chat-section {
  margin-top: 24px;
  background: white;
  border-radius: 16px;
  border: 1.5px solid #f0e8e0;
  padding: 20px;
}

.chat-messages {
  height: 320px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-bottom: 16px;
  padding-right: 4px;
}

.chat-empty {
  text-align: center;
  color: #bbb;
  font-size: 0.9rem;
  margin: auto;
}

/* 메시지 행 */
.chat-message {
  display: flex;
  align-items: flex-end;
  gap: 8px;
}

.chat-message.mine {
  flex-direction: row-reverse;
}

/* 아바타 */
.chat-avatar img,
.chat-avatar-placeholder {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  object-fit: cover;
  flex-shrink: 0;
}

.chat-avatar-placeholder {
  background: #f0e0d6;
  color: #e85d26;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.85rem;
  font-weight: 700;
}

/* 닉네임 + 말풍선 묶음 */
.chat-bubble-wrap {
  display: flex;
  flex-direction: column;
  gap: 3px;
  max-width: 65%;
}

.chat-message.mine .chat-bubble-wrap {
  align-items: flex-end;
}

.chat-nickname {
  font-size: 0.75rem;
  color: #999;
  padding-left: 4px;
}

/* 말풍선 행 (삭제버튼 + 말풍선) */
.chat-bubble-row {
  display: flex;
  align-items: flex-end;
  gap: 6px;
}

.chat-message.mine .chat-bubble-row {
  flex-direction: row-reverse;
}

/* 말풍선 */
.chat-bubble {
  padding: 10px 14px;
  border-radius: 18px;
  font-size: 0.9rem;
  line-height: 1.45;
  word-break: break-word;
  white-space: pre-wrap;
}

.chat-message.others .chat-bubble {
  background: #f5f5f5;
  color: #333;
  border-bottom-left-radius: 4px;
}

.chat-message.mine .chat-bubble {
  background: linear-gradient(135deg, #ff7b54, #e85d26);
  color: white;
  border-bottom-right-radius: 4px;
}

/* 삭제 버튼 */
.chat-delete-btn {
  font-size: 0.7rem;
  color: #ccc;
  background: none;
  border: none;
  cursor: pointer;
  padding: 2px 4px;
  flex-shrink: 0;
  transition: color 0.15s;
}

.chat-delete-btn:hover {
  color: #e53e3e;
}

/* 입력창 */
.chat-input-row {
  display: flex;
  gap: 8px;
}

.chat-input {
  flex: 1;
  padding: 12px 14px;
  border: 1.5px solid #e8ddd8;
  border-radius: 10px;
  font-size: 0.9rem;
  font-family: inherit;
  outline: none;
  transition: border-color 0.2s;
}

.chat-input:focus {
  border-color: #e85d26;
}

.chat-send-btn {
  padding: 12px 20px;
  background: linear-gradient(135deg, #ff7b54, #e85d26);
  color: white;
  border: none;
  border-radius: 10px;
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  transition: opacity 0.15s;
}

.chat-send-btn:hover {
  opacity: 0.9;
}

.chat-send-btn:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}
```

**Step 2: 브라우저에서 스타일 확인**

- 내 메시지: 오른쪽 오렌지 말풍선
- 상대 메시지: 왼쪽 회색 말풍선 + 닉네임 + 아바타
- 삭제 버튼: 내 말풍선 왼쪽에 작게 표시, hover 시 빨간색

**Step 3: Commit**

```bash
git add src/App.css
git commit -m "feat: add chat styles"
```

---

### Task 5: 최종 검증

**Step 1: 두 브라우저 탭(또는 시크릿 모드)으로 동일 방 접속**

- 탭 A에서 메시지 전송 → 탭 B에 즉시 표시되는지 확인
- 탭 A에서 자신의 메시지 삭제 → 양쪽에서 사라지는지 확인
- 탭 B의 메시지에 탭 A에서 삭제 버튼이 없는지 확인
- 메시지 많을 때 자동 스크롤 동작 확인

**Step 2: 방 삭제 후 Firebase 확인**

방 삭제 시 `rooms/{roomId}/messages` 도 함께 삭제됨 (기존 `deleteRoom`이 루트를 삭제하므로 자동).

**Step 3: Final commit**

```bash
git add -A
git commit -m "feat: room chat complete"
```
